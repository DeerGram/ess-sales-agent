{
  "name": "ember",
  "version": "0.1.0",
  "private": true,
  "description": "EMBER - Adaptive AI Agent Platform",
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@9.10.0",
  "scripts": {
    "format": "prettier --write .",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev": "pnpm --filter frontend dev"
  },
  "devDependencies": {
    "eslint": "^9.12.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.3.2",
    "typescript": "^5.4.5"
  },
  "lint-staged": {
    "*.{js,ts,tsx,json,md,css}": [
      "prettier --write"
    ]
  }
}
